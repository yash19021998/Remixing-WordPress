import type { Flags } from "./flags";
declare type Args<TransformOptions> = {
    transformPath: string;
    files: string[];
    flags: Flags;
    transformOptions?: TransformOptions;
};
export declare const run: <TransformOptions>({ transformPath, files, flags: { debug, dry, print, runInBand }, transformOptions, }: Args<TransformOptions>) => boolean;
export {};
